## Запуск тестов и формирование отчета Allure

В этом разделе описано, как выполнить тесты и создать детальный отчет о их результатах с помощью Allure.

### Предварительные требования

Прежде чем начать, убедитесь, что на вашем компьютере установлены и настроены все необходимые компоненты:

1.  **Python и зависимости:** Все библиотеки из `requirements.txt` должны быть установлены. Обычно это делается командой:
    ```bash
    pip install -r requirements.txt
    ```
2.  **Allure Framework:** Необходимо установить командную строку `allure`.
    *   **Инструкция по установке:** Официальное руководство можно найти на [главной странице документации Allure](https://docs.qameta.io/allure/).
    *   **Для пользователей macOS (через Homebrew):**
        ```bash
        brew install allure
        ```
    *   **Для пользователей Windows:** Скачайте последнюю версию с [официального репозитория](https://github.com/allure-framework/allure2/releases) и добавьте путь к папке `bin` в переменную окружения `PATH`.

### 🚀 Запуск тестов и генерация данных для отчета

Тесты запускаются с помощью `pytest`. Для того чтобы Allure мог сформировать отчет, необходимо сохранить результаты выполнения в специальную папку. Для этого используется параметр `--alluredir`.

**Основная команда для запуска:**
```bash
pytest --alluredir=allure-results